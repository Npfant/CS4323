//Author Name: Luis Guzman
//Program Description: Figure out train behavior for trains.
//Pretty much I've set up a code for the train behavior so Nolan and Ashton can merge their code with mine for it to work.

#define NUM_TRAINS 4 //Number of Trains

#define MAX_A 1
#define MAX_B 2
#define MAX_C 1 //Intersection max concurrent capacity
#define MAX_D 3
#define MAX_E 1

sem_t *sem_A, *sem_B, *sem_C, *sem_D, *sem_E; //Semaphore ID's, I would assume we'll use this or mutexes

//function for each train to move through its intersections
void move_train(){
    printf("Train goes somewhere ____"); //pretend it works and this will be the output
    if(strcmp(intersection, "Intersection A") == 0){
         //Ashton figured out this part, Luis was just trying to find an idea on how its setup
        //time passing time will change, most likely mutex/semaphore will be in here, ask Group
    }
    else if("Train")){
       //Ashton figured out
    }
    else if("Train"){
        //Ashton figured out
    }
    else if("Train"){
        //Ashton figured out
    }
    else if("Train"){
        //Ashton figured out
    }
}

//Defines the route each train takes by calling move_train with the intersections it must pass through, based on the group project’s specified directions.
void train_behavior("variables here"){
    if("Train1"){ //basically if this train, it'll go where it needs to go
        move_train(train_name, "Intersection A"); //Luis implemented where the train will go from the instructions of the group project.
        move_train(train_name, "Intersection B");
        move_train(train_name, "Intersection C");
    }
    else if("Train2"){
        move_train(train_name, "Intersection B");
        move_train(train_name, "Intersection D"); //shown in group project for direction
        move_train(train_name, "Intersection E");
    }
    else if("Train3"){
        move_train(train_name, "Intersection C");
        move_train(train_name, "Intersection D"); //shown in group project for direction
        move_train(train_name, "Intersection A");
    }
    else if("Train4"){
        move_train(train_name, "Intersection E");
        move_train(train_name, "Intersection B"); //shown in group project for direction
        move_train(train_name, "Intersection D");
    }
}

int main(){
    /*
    
    This part is where Nolan will implement his fork process by merging my train behavior code with his.
    Ashton will take Nolan's and I combined code to merge it with his since he figured out the semaphores.
    
    */

}

/*
SHARED MEMORY possibly fixes the issue

Intersection* intersections; //put at very top. this will go for for the function voidcreateSharedIntersections()
createSharedIntersections //This will be in the main function

//this will be in main function but after opening the intersection and train file
pthread_mutexattr_t mattr;
    pthread_mutexattr_init(&mattr); //initializes as process shared
    pthread_mutexattr_setpshared(&mattr, PTHREAD_PROCESS_SHARED);
*/
